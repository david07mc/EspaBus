package domain;

/**
 * This class it's the intermediary between the GUI and the information. It has methods to generate a list of pieces if doesn't exists yet and if it does, it has another method to adding new pieces. Both can be useful for 2 use cases, "generate a piece list" and "ask for bill", as they are both lists (the only thing is that they have different content).
 */
public class PieceController {

	public void generatePiecesList() {
		throw new UnsupportedOperationException();
	}

	public void addPiece(Piece aPiece) {
		throw new UnsupportedOperationException();
	}
}